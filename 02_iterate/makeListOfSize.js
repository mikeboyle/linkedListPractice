const { Node } = require('../01_node/node');
const { printLinkedList } = require('./printLinkedList');

/**
 * Make a linked list of size n.
 * The value of each node in the list should increment by one
 * from 0 to n-1.
 * Example: makeListOfSize(5); // 0 -> 1 -> 2 -> 3 -> 4
 */
const makeListOfSize = n => {
  // your code here
};

printLinkedList(makeListOfSize(5));
printLinkedList(makeListOfSize(13));
